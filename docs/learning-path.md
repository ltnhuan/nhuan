# Learning Path Guide

Open the Learning Path tab in Course Studio and choose a mode: free, sequential, prerequisite, mastery, or adaptive. Rules target a course, section, or component and contain JSON requirements such as component completion, quiz score minimum, video watch percentage, or manual approval.

Learner progress pages call access-check endpoints before opening content. Completion requests always pass through the server-side CompletionEngineService.
